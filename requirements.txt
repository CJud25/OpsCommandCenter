# Core stack. Pinned to compatible ranges so a fresh install matches what was
# tested (Streamlit 1.58, pandas 3.0, numpy 2.5, plotly 6.8 on Python 3.14).
streamlit>=1.49,<2
pandas>=2.2,<3.1
numpy>=1.26,<3
plotly>=5.22,<7
